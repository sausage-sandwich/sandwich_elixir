= link "<- Back", to: recipes_path(@conn, :index)

= form_for @changeset, recipes_path(@conn, :create), fn f ->
  .form-group
    = label f, :title, class: "control-label"
    = text_input f, :title, class: "form-control"
    = error_tag f, :title

    .form-inline.js-repeatable
      = inputs_for f, :recipe_ingredients, fn p ->
        .js-repeatable-block.form-group
          = select p, :ingredient_id, Map.new(@ingredients, &({&1.title, &1.id})), class: "form-control"
          = number_input p, :quantity, class: "form-control"
          = select p, :unit, units, class: "form-control"
        .js-repeatable-block-add-button.btn.btn-warning Add

  .form-group
    = label f, :body, class: "control-label"
    = textarea f, :body, class: "form-control"
    = error_tag f, :body

  .form-group
    = submit "Submit", class: "btn btn-primary"
